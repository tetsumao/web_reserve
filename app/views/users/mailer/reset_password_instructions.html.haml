%p こんにちは #{@resource.email}!
%p パスワードリセットのリクエストが行われました。以下のリンクをクリックしてパスワードを変更できます。
%p= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token)
%p もしあなたがパスワードリセットをリクエストしていない場合は、このメールを無視して下さい。
%p 上記リンクをクリックして新しいパスワードを作成しない限り、パスワードは変更されません。
